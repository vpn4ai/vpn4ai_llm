{
  "name": "vpn4ai-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "eleventy --serve --watch",
    "build": "node -e \"require('fs').rmSync('_site', { recursive: true, force: true })\" && eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
